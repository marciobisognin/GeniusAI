{
  "name": "genius-allspark",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo do Genius Allspark Canvas — packages/* (lógica compartilhada) e apps/* (aplicações).",
  "bin": {
    "genius-allspark": "./bin/genius-allspark.mjs"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/canon -w packages/providers -w packages/agent-library -w packages/execution -w packages/learning -w packages/constructor -w apps/canvas",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
